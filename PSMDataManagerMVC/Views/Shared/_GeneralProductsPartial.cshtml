@model PSMDataManagerMVC.Models.ProductViewModel
    @foreach (var item in Model.Products)
    {
        <div class="btn btn-success btn-lg float-left mr-1 mb-1">
            <div class="row">@item.Name</div>
            <div class="row text-primary">@item.Description</div>
        </div>
    }
