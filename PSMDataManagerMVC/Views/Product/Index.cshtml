@model List<PSMDataManagerMVC.Models.ProductViewModel>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="container">
    @foreach (var item in Model)
    {
        {
            Html.RenderPartial("_ProductPartial", item);
        }
    }
</section>

